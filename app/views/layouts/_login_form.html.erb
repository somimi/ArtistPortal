<h3>Register/Sign In</h3>
<small>All artists must register for 2012 in order to sign in and apply.  If you have not registered, please click on the link below and complete the registration process.</small><br />
<p><%= link_to('Register', new_user_registration_path)  %><br />
	<small>Already registered for 2012?  Sign in below.</small>
<%= form_for("user", :url => user_session_path) do |f| %>
  <p><%= f.label "Email" %></p>
  <p><%= f.text_field :email, :size => "20" %></p>
  <p><%= f.label "Password" %><p>
  <p><%= f.password_field :password, :size => "20" %></p>
  <%= f.check_box :remember_me %>
  <%= f.label :remember_me %>
  <p><%= f.submit 'Sign in' %></p>
  <%= link_to "Forgot your password?", new_password_path('user') %>
  
<% end %>