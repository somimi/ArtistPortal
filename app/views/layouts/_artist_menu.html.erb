<% if !current_user.artist %>
	<%= link_to "Add Profile", new_artist_path %>
<% else %>
    <%= link_to "Edit Profile", edit_artist_path(current_user.artist) %>
<% end %>

<% if current_user.artist %>
	<% if current_user.artist.paid_visual? %>
		<%= link_to "Visual Art", visual_submissions_path, :var => "something" %>
	<% else  %>
		<%= link_to "Pay for Visual Submission" %>
	<% end %>

	<% if current_user.artist.paid_literary? %>
		<%= link_to "Literary Art" %>
	<% else %>
		<%= link_to "Pay for Literary Submission" %>
	<% end %>

		<%= link_to "Film Art" %>
		<%= link_to "Installation Art" %>
<% end %> 