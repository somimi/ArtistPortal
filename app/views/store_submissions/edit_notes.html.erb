<%= form_for(@store_submission, :url => store_submission_path(@store_submission)) do |f| %>
  <%= f.error_messages %>
<fieldset>
	<legend>Notes</legend>
	<p>
		<%= f.text_area :admin_notes %>
	</p>
</fieldset>
  <p><%= f.submit "Update Notes" %></p>
<% end %>