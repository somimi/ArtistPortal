<h1>Performance Submissions</h1>

<table id="performance-index" class="display">
  <thead>
	  <tr>
	    <th>Title</th>
		<th>First Name</th>
		<th>Last Name</th>
		<th>Email</th>
		<th>Title</th>
		<th>Acceptance Status</th>
	  </tr>
  </thead>
  <tbody>
<% @performance_submissions.each do |performance_submission| %>
  <tr>
	<td><%= link_to performance_submission.title, edit_performance_submission_path(performance_submission) %></td>
	<td><%= link_to performance_submission.artist.first_name, artist_path(performance_submission.artist) %></td>
	<td><%= link_to performance_submission.artist.last_name, artist_path(performance_submission.artist) %></td>
	<td><%= link_to performance_submission.artist.email, artist_path(performance_submission.artist) %></td>
	<td><%= best_in_place performance_submission, :acceptance_status, :type => :select, :collection => [["Undecided", "Undecided"],["Denied", "Denied"],["Accepted", "Accepted"]], :nil => performance_submission.acceptance_status %></td>	
  </tr>
<% end %>
</tbody>
</table>
<br />
<% if current_user.is_artist? %>
	<p><%#= link_to 'New Performance submission', new_performance_submission_path %></p>
<% else %>
<% end %>

<b>Submission Requirements:</b>
<ul>
	<li>Performers must apply by the deadline. Incomplete applications will not be considered.</li>
	<li>Performers must be willing to work with the Performance Director and/or choreographer to adapt their acts for the Festival.</li>
	<li>Selected performers must be available for conceptual meetings in late-April and for rehearsals, which are scheduled to begin in May.</li>
	<li>Performers must be at least 18 years old. All body types, ages (over 18), genders, and ethnicities are encouraged to apply.</li>
	<li>Auditions will be required.</li>
</ul>